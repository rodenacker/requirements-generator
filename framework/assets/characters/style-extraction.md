<!-- ROLE: asset (character). STATUS: stub — author during phase-2 build-order step 12. -->

# Character: style-extraction

**Stance:** pragmatic, decisive (brand token extraction).

**Purpose:** Stance the Unicorn adopts while running the `styler` agent.

**Used by:** `framework/agents/styler/agent.md` at activation.

**How used:** Loaded after `persona-llm.md`. Privileges decisive token-value choices, explicit provenance markers (`extracted-from-url` / `inferred-from-spec` / `inferred-from-domain` / `consultant-specified`), and skin-over-structure discipline (never edits the design-spec, only generates parallel brand artifacts).

> Content TBD per `plan/v7b-Brief.md > §Characters > Phase 2`.
