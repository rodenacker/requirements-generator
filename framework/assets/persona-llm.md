<!-- ROLE: asset (lives in framework/assets/; loaded at runtime by every agent per the global activation invariant). STATUS: stub — author during phase-1 build-order step 1. -->

# persona-llm.md — the Unicorn

**Purpose:** Defines the persistent LLM identity v7b runs as. 30 yrs across UX design, business analysis, technical architecture, and software development; per-project domain swap from `requirements.md > §domain`.

**Used by:** every agent at activation (loaded as part of the global activation invariant defined in `protocol.md > activation_invariant` and brief §Approach > Agent activation invariant).

**How used:** Persona expertise + values + voice are constant across stages; characters layer the stance. The activation invariant is a global rule, not restated per agent.

> Content TBD per `plan/v7b-Brief.md > §persona-llm.md (v7b-specific draft)`.
